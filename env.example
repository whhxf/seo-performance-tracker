# SEO Performance Tracker — 环境变量示例
# 使用方式：在本 skill 目录下复制为 .env 并填写，不要提交 .env 到 Git
# 说明见 README.md 与 SKILL.md

# ========== Google（GSC / GA4）==========
# GCP Service Account JSON 的路径。推荐放在本 skill 的 credentials/ 下，便于本地测试
# 相对路径：以本 skill 目录为工作目录时用 credentials/gcp-service-account.json
# 创建方式见 SKILL.md「第一步：Google Cloud Console」
GCP_CREDENTIALS_PATH=credentials/gcp-service-account.json

# ========== Bing Webmaster ==========
# 在 Bing Webmaster Tools → 设置 → API access 中生成
BING_WEBMASTER_API_KEY=

# ========== 站点与脚本（可选）==========
# 默认站点代号，与 GSC/GA4 中的站点或 Property 对应（由你在 GSC/GA4 中自行约定）
# 例如：default、example.com、my-site
DEFAULT_SITE=default

# 若脚本不在本 skill 的 scripts/ 下，可指定工作区目录（Agent 在此目录下执行 node scripts/xxx）
# 留空则使用本 skill 目录或当前项目根目录
# WORKSPACE_DIR=
